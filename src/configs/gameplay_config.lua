GAMEPLAY_CONFIG = {
    -- player game zone, restricts movement
    GAME_ZONE = { x=16, y=20, w=100, h=80 },
    
    PROJECTILE_TYPES = {
        BULLET = 1,
        LASER = 2,
    },

    -- background patches that changes mechanics of entities
    PATCH_TYPES = {
    }
}